
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ with MSVC",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\slot_machine.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "Build C++ with MSVC"
        }
    ]
}
